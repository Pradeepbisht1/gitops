apiVersion: v1
kind: Service
metadata:
  name: argocd-sync-to-healthy-exporter
  namespace: argocd
  labels:
    app: sync-to-healthy-exporter
spec:
  selector:
    app: sync-to-healthy-exporter
  ports:
  - name: http
    port: 8000
    targetPort: 8000
